# HU-006 - Resultados y tabla de posiciones

## Historia
Como promotor de un torneo en curso
Quiero cargar los resultados de los partidos
Para que el sistema actualice la tabla de posiciones y los participantes puedan consultarla.

## Historia (segunda perspectiva)
Como jugador, equipo inscrito o visitante del torneo
Quiero ver la tabla de posiciones y el historial de resultados
Para seguir el avance y el rendimiento de los participantes.

## Criterios de aceptación (GIVEN/WHEN/THEN)
1. Carga de resultado por el promotor
- GIVEN un promotor dueño del torneo con un partido pendiente
  WHEN registra marcador final y, opcionalmente, goleadores
  THEN el sistema persiste el resultado, recalcula la tabla de posiciones y publica el cambio en la ficha del torneo.

2. Consulta pública de la tabla
- GIVEN un torneo publicado con al menos un resultado cargado
  WHEN un visitante (autenticado o no) accede a la ficha del torneo
  THEN el sistema muestra la tabla de posiciones con columnas estándar (PJ, PG, PE, PP, GF, GC, DG, Pts).

3. Actualización reactiva
- GIVEN un usuario viendo la tabla de posiciones
  WHEN el promotor carga un nuevo resultado
  THEN la tabla se actualiza en la siguiente consulta (no se requiere tiempo real en MVP1; refresh aceptable).

4. Reflejo de stats básicas en el perfil
- GIVEN un jugador miembro de un equipo inscrito
  WHEN el torneo registra un partido con su participación
  THEN el sistema suma los contadores básicos del perfil (partidos jugados, victorias, goles) visibles en la ficha según la visibilidad configurada (RF-002).

5. Corrección de resultado cargado erróneamente
- GIVEN un promotor que detectó un error en un resultado
  WHEN edita el resultado dentro de la ventana de corrección
  THEN el sistema recalcula la tabla y registra un evento de auditoría del cambio.

## Dependencias
- RF-005 (`docs/01-requisitos-funcionales.md`).
- HU-004 (torneo publicado), HU-005 (equipos/jugadores inscritos).

## Tareas técnicas
- [x] DB: migración `apps/web/supabase/migrations/20260429140000_g4_sprint5_standings.sql` con:
  - enum `match_status_v2 (programado|en_juego|finalizado|w_o|cancelado)` y `match_event_type (gol|auto_gol|amarilla|roja|sustitucion)`.
  - tabla `tournament_matches` con FKs a `tournaments` y `tournament_registrations`, índices `(tournament_id, round)`, `(scheduled_at)`, `(status)`, CHECK `home<>away`, CHECK de coherencia `scores ↔ status`, RLS por owner con `correction_window_ends_at`.
  - tabla `match_events` con RLS `all` solo para owner del torneo.
  - vista materializada `public.standings` (tournament_id, registration_id, played, wins, draws, losses, goals_for, goals_against, goal_difference, points) con índice único `standings_unique_row` requerido por `REFRESH MATERIALIZED VIEW CONCURRENTLY`.
  - función `public.refresh_standings()` con `SECURITY DEFINER` (granteada solo a `authenticated`).
  - trigger `tm_before_*_enforce_finalize` que valida que el torneo esté en `cerrado_inscripciones` o `finalizado` al marcar un match como `finalizado`, y setea `correction_window_ends_at = now()+48h` si no viene.
  - trigger `tm_after_finalize_refresh` que llama a `refresh_standings()` después de insert/update relevantes; captura cualquier excepción para no romper la transacción principal.
- [x] Backend: `apps/web/lib/tournaments/matches.ts` con `createMatch`, `listMatches`, `getMatchById`, `recordResult`, `addMatchEvent`, `listMatchEvents`, `listStandings`, `mapResultError` y algoritmo puro `computeStandingFromMatches(tournament_id, registration_id, matches[])` para cálculo cliente / tests.
- [x] Schemas Zod en `apps/web/lib/validation/schemas.ts`: `matchStatusEnum`, `matchEventTypeEnum`, `matchCreateSchema` (con refine `home ≠ away`), `matchResultSchema`, `matchEventSchema`.
- [x] Frontend:
  - `/tournaments/[id]/matches` — fixture público con badges de estado por match, acciones del promotor ("Cargar resultado" / "Editar").
  - `/tournaments/[id]/matches/new` — form del promotor con validación de 2+ inscripciones confirmadas.
  - `/tournaments/[id]/matches/[matchId]` — carga de resultado + historial de eventos (goles, tarjetas, etc.) para el promotor.
  - `/tournaments/[id]/standings` — tabla de posiciones pública (PJ, G, E, P, GF, GC, DG, Pts).
  - `/tournaments/[id]` (detalle) ahora tiene botones "Partidos" y "Tabla de posiciones" para cualquier viewer.
- [x] QA: `apps/web/tests/lib/matches.test.ts` con 13 tests — validación (home=away, scores negativos, minuto>130, sin auth), mapping de errores (`P0001/tournament_not_ready_for_results`, `42501/permission`), algoritmo puro de standings (V/E/D, acumulado, ignora no-finalizados, ignora otros torneos, ignora no-participantes).

## Notas de alcance (Opción A Acotada)
- **Refresh de standings**: se hace completo en cada finalización/edición (aceptable para MVP1 según `db/data-model.md` §3.15). El post-MVP evaluará tabla regular con triggers granulares si crece el volumen.
- **Proyección de stats al perfil del jugador** (Bloque 4) queda para Sprint 6 — requiere mapear `match_events.player_id` → `profiles_core` y definir agregación (partidos jugados, goles, tarjetas). Por ahora los eventos quedan almacenados y consultables por el promotor.
- **Desempates** (§3.15): el ORDER BY por `points → goal_difference → goals_for` está en `listStandings`. No se usan los `tiebreaker_rules jsonb` del PRD porque la tabla `tournaments` del MVP actual no lleva esa columna (sería un cambio de modelo fuera de scope).
- **Fixture automático** no está implementado: los partidos se crean uno a uno por el promotor. La generación de fixtures (liga todos-contra-todos, fase de grupos) es post-MVP.

## Evidencias
- PR: #XX — `feat(g4-sprint5): HU-006 Resultados + Standings (RF-005)` (actualizar al abrir PR).
- Tests: `pnpm test` → 153/153 PASS (13 tests nuevos en `tests/lib/matches.test.ts`).
- Lint/Typecheck: `pnpm lint` 0 errors / 15 warnings preexistentes · `pnpm typecheck` OK.
- Build: `pnpm build` OK. Rutas generadas: `/tournaments/[id]/matches`, `/tournaments/[id]/matches/new`, `/tournaments/[id]/matches/[matchId]`, `/tournaments/[id]/standings`.
- UAT:
