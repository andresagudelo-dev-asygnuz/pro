# HU-004 - Creación y gestión de torneos (promotor)

## Historia
Como promotor autenticado
Quiero crear y configurar un torneo de fútbol con datos mínimos (nombre, formato, cupos, ubicación, fechas, categorías)
Para publicarlo y permitir que jugadores o equipos se inscriban.

## Criterios de aceptación (GIVEN/WHEN/THEN)
1. Creación de torneo con datos mínimos
- GIVEN un promotor autenticado
  WHEN envía el asistente de creación de torneo con los datos mínimos válidos (nombre, formato, cupos, ubicación, fecha)
  THEN el sistema crea el torneo en estado `borrador` y lo muestra en el panel del promotor.

2. Publicación del torneo
- GIVEN un promotor con un torneo en estado `borrador`
  WHEN publica el torneo
  THEN el sistema pasa el torneo a estado `abierto a inscripciones`, lo hace visible en el listado público y habilita el flujo de inscripción (HU-005).

3. Edición de torneo en borrador
- GIVEN un promotor con un torneo en estado `borrador`
  WHEN edita los datos del torneo
  THEN el sistema persiste los cambios sin notificar a terceros (aún no hay inscritos).

4. Edición de torneo publicado
- GIVEN un promotor con un torneo ya publicado y con inscritos
  WHEN edita datos no-estructurales (descripción, banner)
  THEN el sistema persiste los cambios y registra un evento de auditoría.
  AND WHEN intenta editar datos estructurales (formato, cupos a la baja, fechas ya pasadas) sin justificación THEN el sistema pide confirmación y, si confirma, notifica a los equipos inscritos.

5. Restricción por rol
- GIVEN un usuario autenticado sin rol promotor
  WHEN intenta acceder al asistente de creación de torneo
  THEN el sistema bloquea el acceso y muestra mensaje de permisos.

## Dependencias
- RF-003 (`docs/01-requisitos-funcionales.md`).
- HU-001 (cuenta con rol promotor).

## Estado: **Implementado base — Sprint 3 / restaurado en Sprint 4**

## Tareas técnicas
- [x] Backend (server actions / lib): `createTournament`, `getTournaments`, `getMyTournaments`, `publishTournament` en `apps/web/lib/tournaments/api.ts`.
- [x] Backend: modelo `tournaments(id, owner_id, name, format, slots, slots_filled, location, start_date, end_date, status, categories, created_at, updated_at)` con enums `tournament_status` y `tournament_format`.
- [x] Backend: RLS por `owner_id` y visibilidad pública para estados publicados; checks de `end_date >= start_date` y `slots_filled <= slots`.
- [x] Frontend: formulario `/tournaments/new` (single-step) y panel `/tournaments/mine`. _Pendiente refinar a multi-step y agregar acciones (cerrar inscripciones, cancelar) en sprints siguientes._
- [x] Frontend: listado público `/tournaments`. _Pendiente filtros por ubicación / estado._
- [x] DB: migración `apps/web/supabase/migrations/20260425140000_g4_sprint3_tournaments.sql` + índices por `status`, `owner_id`, `start_date`.
- [x] QA: tests unitarios `apps/web/tests/lib/tournaments-api.test.ts` y `tournaments.test.ts` (esquemas + lógica de transiciones de estado).
- [ ] QA: e2e de creación + publicación (movido a Sprint 5 / G5).

## Pendiente para sprints siguientes
- Asistente multi-step para creación.
- Acciones de gestión: editar, cerrar inscripciones, cancelar (transiciones de estado adicionales).
- Listado con filtros por ubicación y estado.
- Auditoría de cambios estructurales en torneos publicados con notificación a inscritos (depende de HU-005).

## Evidencias
- PR original: [#20](https://github.com/andresagudelo-dev-asygnuz/pro/pull/20) — `feat: HU-004 Create and manage tournaments` — mergeado 2026-04-26 (commit `c41bf1c`).
- Regresión: el merge del PR #22 (landing premium) borró los archivos de HU-004 en `main`. Ver `memory/daily/2026-04-28.md` y `tasks/sprint-week-04.md`.
- PR de restauración: este PR (cherry-pick de `c41bf1c` + migración reubicada a `apps/web/supabase/migrations/` + lint/typecheck/tests/build verificados).
- Tests: `apps/web/tests/lib/tournaments-api.test.ts`, `apps/web/tests/lib/tournaments.test.ts` — 130/130 pasan al cierre de Sprint 4.
- UAT: pendiente para G5.
