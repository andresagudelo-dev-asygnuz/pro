# 2026-04-16

## Qué se hizo

- Revisados `docs/intake/00-indice-y-alcance.md` y `docs/intake/03-propuesta-valor-y-mvp.md` para analizar trade-offs entre Opción A (torneos + social amplia) y Opción B (app hiperlocal Manizales).
- **Decisión MVP cerrada:** Híbrido "Opción A Acotada" — núcleo de torneos + perfil tipo ficha (RF-001 a RF-005), solo fútbol +18, lanzamiento geográfico en Eje Cafetero.
- Actualizado `docs/intake/00-indice-y-alcance.md`: decisión MVP, lista fuera de alcance, tabla de revisión, índice.
- Actualizado `docs/intake/03-propuesta-valor-y-mvp.md`: justificación de 5 puntos, KPIs con objetivos iniciales, hipótesis ajustadas.
- Actualizado `docs/00-prd.md`: secciones 2–7 (objetivo, escenario, alcance, hipótesis, criterios de éxito, riesgos).
- Agregado **RF-005 (Resultados y tabla de posiciones)** en `docs/01-requisitos-funcionales.md` con criterios GWT.
- Agregado umbral de rendimiento (p95 LCP < 2 s, TTFB < 500 ms) en `docs/02-requisitos-no-funcionales.md`.
- Marcado G1 como "Listo para revisión" en `tasks/gate-status.md`.
- Resuelto conflicto de merge con main: integrada migración completa de RF del monolito (RF-006 a RF-018) renumerando para preservar RF-005 MVP.
- Corregidas referencias obsoletas señaladas por Devin Review (RF-018 rango, RF-010/RF-015 en nota de RF-005).

## Bloqueos

- Ninguno.

## Decisiones

- MVP = Opción A Acotada (5 RF: registro, perfil, torneo, inscripción, resultados).
- Fuera de MVP: live streaming, feed social, chat, pagos, árbitros, multi-deporte, mapa/discovery.
- KPIs iniciales: 50 registros, 5 torneos, 30% WAU/MAU, 3 organizadores activos en 4 semanas.
- Renumeración de RF del monolito: RF-005 original (Validación y calificación) → RF-006; RF-017 original (Soporte) → RF-018.

## Próximo paso

- Fundador aprueba G1 y merge del PR.
- Iniciar G2 (Diseño): flujos de usuario y wireframes para los 5 RF del MVP.

---

## Nota — build MVP v0 "Opción A Acotada ampliada" (mergeado a `main`)

En paralelo se construyó y mergeó a `main` un MVP v0 ejecutable bajo `apps/web/` (PR #5) para empezar a validar las 3 hipótesis (confianza/coordinación/matching) con usuarios reales. Alcance ampliado vs. el MVP del PRD (incluye chat y matching por deporte/ciudad, no solo torneos). Esto **no** implica avance de gates — G1 sigue pendiente de aprobación formal.

### Entregado

- Scaffold Next.js 16 (App Router, RSC, Tailwind v4, shadcn/ui base-nova) en `apps/web/`.
- Supabase real conectado (`ewzpwldtaeaxtesimjau`): 6 tablas con RLS, triggers de integridad (capacity `FOR UPDATE`, rating_avg/count, matches_played, auto-profile).
- Auth email+password con PKCE, `emailRedirectTo` resuelto por request (soporta localhost/preview/prod sin recompilar).
- Flujos: signup → onboarding → feed → crear partido → unirse → chat realtime → perfiles.
- Fix de chat realtime: polling 2s + canal por mount + barra de diagnóstico `rt·polls·last·msgs`.
- Hardening (fases A+B+C+D del reporte de auditoría): Zod centralizado, `withAuth`, `mapDbError`, `env.ts` con `getAllowedOrigins()`, rate limiting DB (`check_rate_limit`), authz defense-in-depth en `sendMessage`, `leaveMatch` bloquea organizador, caps de longitud DB, passwords min 8.
- Fix `formDataToObject` filtra `$ACTION_*` keys (sin esto `createMatch` fallaba con `.strict()`).
- 62 unit tests (vitest) en CI, metadata/OG, favicon, responsive.
- Deploy Vercel en cuenta del fundador (`andresagudelo-asygnuz/pro-web`).

### Evidencia

- E2E grabado en preview `pro-9y0laldq4-andresagudelo-asygnuz.vercel.app`: 6/6 assertions pasaron (chat realtime A↔B ≤3s sin reload, error de longitud, regresión "Editar perfil").

### Pendientes para abrir a usuarios

1. Conectar repo GitHub ↔ Vercel (instalar Vercel GitHub App + link en Settings → Git).
2. Supabase → Authentication → URL Configuration: sumar `https://*.vercel.app/**` a Redirect URLs y setear Site URL al dominio final cuando exista.
3. Decisión: dejar "Confirm email" on (más seguro) vs. off (menos fricción para validar).
4. Custom domain en Vercel.

### Deuda técnica conocida

- Transición automática de `status` del partido (`open → in_progress → completed`) no implementada.
- Rate limiting testeado unitariamente, no con carga real.
- Subida de avatar a Supabase Storage pendiente (hoy solo URL).
- Google OAuth pendiente.
- No hay tests E2E automatizados (Playwright), solo el E2E manual grabado.
