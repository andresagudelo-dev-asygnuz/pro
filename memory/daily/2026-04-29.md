# Daily — 2026-05-01

## Contexto

Cierre de G4 con Sprint 6 merged. Arranca **G5 QA (Fase 1)**: cobertura
unitaria, test plans formales por HU y scripts SQL de verificación
RLS/triggers.

## Lo hecho hoy

### 1. Cobertura unit `lib/tournaments/*` elevada a ≥80%

Antes: 46% global del folder (api.ts 27%, matches.ts 61%, registrations.ts 40%).
Después: **88% global** (api.ts 91%, matches.ts 94%, registrations.ts 82%).

- `tests/lib/tournaments-api.test.ts`: de 2 → **11 tests**. Cubre
  createTournament (auth, validación, happy, error mapping), getTournamentById,
  getTournaments, getMyTournaments (filter owner), publishTournament
  (borrador → abierto).
- `tests/lib/matches.test.ts`: de 12 → **24 tests**. Cubre createMatch
  (auth + zod + error map), recordResult (P0001/42501), addMatchEvent,
  listMatches/getMatchById/listMatchEvents/listStandings (chains de order
  verificadas) + computeStandingFromMatches (V/E/D, ignorados, acumulado).
- `tests/lib/registrations.test.ts`: de 9 → **25 tests**. Cubre
  createTeam (extras + solo-capitán + auth + error), getMyTeams,
  getTeamMembers (happy + data null), registerSoloToTournament (gate
  verificación + happy), cancelRegistration, listRegistrations,
  getMyRegistrations, findUnverifiedUsers (empty short-circuit + happy + pendientes),
  mapeo `P0001/tournament_not_open`.

Total suite: 153 → **188 tests PASS**.

### 2. Test plans formales por HU

Nuevos bajo `tasks/test-plans/`:
- `README.md` (resumen ejecutivo + tabla de estado por HU).
- `HU-001.md` Signup + roles duales.
- `HU-002.md` Verificación edad + RPC bypass.
- `HU-003.md` Perfil 4 bloques + visibility.
- `HU-004.md` Torneos.
- `HU-005.md` Inscripciones (con 2 casos marcados específicamente como
  hardening del Sprint 6).
- `HU-006.md` Resultados + standings (con caso Sprint 5 fix mat view).

Cada plan mapea casos GIVEN/WHEN/THEN → archivo de cobertura → Estado.

### 3. Scripts SQL de verificación RLS + triggers

`tasks/test-plans/sql/`: 11 scripts idempotentes (tx + rollback) que el
fundador puede correr con `psql` contra Supabase local. Cubren:

- HU-001: user_roles RLS + trigger safeguard.
- HU-002: age_verifications RLS + RPC `find_unverified_users`.
- HU-003: visibility override + escritura ajena.
- HU-004: owner write + borrador ajeno oculto.
- HU-005: enforce_capacity (not_open + full), **🔴 Sprint 6 WITH CHECK por rol**,
  **🔴 Sprint 6 re-confirm bloqueado**, find_unverified_users cross-user.
- HU-006: mat view populada al init + refresh trigger con counters correctos.

Cada script imprime `NOTICE: [PASS]/[FAIL]`.

### 4. Docs actualizados

- `tasks/gate-status.md`: G5 **En curso — Fase 1**.
- `tasks/test-plans/README.md`: tabla ejecutiva con estado por HU.
- Este daily log.

## Blocker / límite de entorno

No pude correr los scripts SQL end-to-end contra Supabase local: el sandbox
del agente no puede pullear `public.ecr.aws/supabase/postgres:17.6.1.106`
(`toomanyrequests: Data limit exceeded`). Los scripts quedan entregados para
que el fundador los corra en su máquina local. Es una tarea de ~5 minutos
por HU.

## Siguiente paso

Abrir PR único con test plans + cobertura + scripts SQL. Esperar a que el
fundador corra los scripts localmente y reporte resultados. Después cerrar
G5 Fase 1 y arrancar Fase 2 (E2E recording + findings residuales Devin
Review de PR #30).
