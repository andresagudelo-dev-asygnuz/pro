# Diario - 2026-04-25

## Tareas completadas
- Fix de tests unitarios: Se corrigieron los fallos causados por variables de entorno faltantes ajustando el archivo `vitest.config.ts` y definiendo las variables necesarias (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`) en un `tests/setup.ts` garantizando que los 130 tests pasaran correctamente.
- Desarrollo de la **HU-004** (Torneos):
  - Migración SQL creada (`db/migrations/20260425140000_g4_sprint3_tournaments.sql`) implementando la tabla base para torneos y sus políticas de seguridad (RLS).
  - Lógica de esquema para validaciones implementada en `apps/web/lib/validation/schemas.ts`.
  - Capa de datos en `apps/web/lib/tournaments/api.ts` con funciones `createTournament`, `getTournaments`, y `getMyTournaments`.
  - Pruebas unitarias para los esquemas y lógica de API.
  - Implementación inicial del UI de torneos en `apps/web/app/(app)/tournaments`:
    - Listado público (`/tournaments/page.tsx`).
    - Panel privado ("mis torneos", `/tournaments/mine/page.tsx`).
    - Formulario de creación (`/tournaments/new/page.tsx`).
- Correcciones de Typescript (Typecheck y Linting).

## Decisiones
- Para poder avanzar con el Sprint 4 y el módulo de resultados (HU-006), me pidieron explícitamente re-habilitar / crear la estructura base para la HU-004 de los torneos porque los archivos no existían en el frontend de la rama actual.

## Siguientes pasos
- Refinar el UI y validación de creación de torneos.
- Implementar la HU-005 (Inscripciones de equipos / jugadores a torneos) para dejar preparado el camino de resultados.
- Pasar a G5 cuando Sprint 4 y el hardening finalicen.
