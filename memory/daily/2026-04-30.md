# 2026-04-30 · Sprint 6 hotfixes cierre HU-005 + arranque post HU-006

## Contexto
- PR #29 (HU-005) mergeado a la tarde; PR #30 (HU-006) fue revisado, se
  encontró un 🔴 en la mat view `standings` (creación `WITH NO DATA` + único
  refresh path con `CONCURRENTLY`). Se arregló con `7e75c7d` y el fundador
  mergeó #30.
- Quedaban pendientes dos 🔴 secundarios de #29 diferidos explícitamente al
  mergear:
  1. Trigger `sync_tournament_slots_on_status_change` sin validación de
     `tournament.status = 'abierto_inscripciones'` al re-confirmar.
  2. RLS `tr_update_self_or_owner` sin `WITH CHECK` por rol.
- El fundador pidió priorizar el Sprint 6 de hotfixes sobre revisar los 6
  findings menores de Devin Review en PR #30.

## Entregado hoy

### Sprint 6 (migración única, acotada)
- `apps/web/supabase/migrations/20260430140000_g4_sprint6_hotfixes_registrations.sql`:
  - `create or replace function sync_tournament_slots_on_status_change` con
    lectura de `status` del torneo bajo el mismo `for update`; raise
    `tournament_not_open` cuando la transición es a `confirmada` sobre un
    torneo ya cerrado. El camino `confirmada → !confirmada` (liberar cupo)
    sigue siendo libre: cancelar un inscripto en `cerrado_inscripciones`
    tiene que seguir funcionando.
  - `drop policy + create policy "tr_update_self_or_owner"` con `WITH CHECK`:
    promotor puede setear `confirmada`/`lista_espera`/`cancelada`;
    `registered_by` solo puede setear `cancelada`. `comment on policy`
    documenta el contrato para que cualquier migración posterior lo respete.
- Sin cambios de TS: `mapRegistrationError` ya mapeaba `tournament_not_open`
  a texto humano desde HU-005.

### Docs sincronizadas
- `tasks/gate-status.md`: G4 pasa a "Sprint 6" con resumen del hotfix.
- `tasks/sprint-week-06.md`: nuevo archivo con alcance, diff semántico,
  validación local y deuda técnica restante.
- `tasks/hu/HU-005.md`: evidencias actualizadas con el link del PR #29
  merged y descripción de los dos hardenings de Sprint 6.

## Validación local
| Check | Resultado |
|---|---|
| `pnpm lint` (apps/web) | 0 errors · 15 warnings (preexistentes) |
| `pnpm typecheck` | OK |
| `pnpm test` | 153/153 PASS |
| `pnpm build` | OK (sin rutas nuevas) |

## Deuda técnica pendiente

1. **6 findings menores de Devin Review en PR #30**: no pude ver los
   detalles porque la SPA `app.devin.ai/review/...` devolvía 500 (error de
   preload de `PRDetailPage-*.css`). La API de GitHub solo expone el 🔴
   principal; los 6 menores son visibles solo dentro de la UI de Devin. Se
   retomará en G5 QA cuando la UI esté disponible o por review manual.
2. **Validar esquema full-rebuild**: como Sprint 6 fue `create or replace`
   sobre la función del Sprint 5, al correr todas las migraciones en orden
   sobre una DB limpia la definición final debería coincidir con la del
   Sprint 6. Vale la pena hacer una corrida limpia local antes de cerrar G4
   para confirmar que no quedó deriva.
3. **Proyección de `match_events.player_id` → stats del perfil (Bloque 4)**
   sigue fuera de scope (pendiente desde HU-006).

## Próximo paso
- Abrir PR Sprint 6 contra `main` y esperar CI verde + Devin Review.
- Una vez mergeado, arrancar G5 QA: plan de pruebas E2E HU-001→HU-006,
  cobertura en `lib/tournaments/*` y validación RLS en ambiente Supabase real.
