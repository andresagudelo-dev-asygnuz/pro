# Diario — 2026-04-26

## Qué se hizo

- **Sprint 2 cerrado**: PRs #15 (DB foundation perfil) → #16 (Bloque 1 Identidad) → #17 (Bloques 2/3/4 + preview por audiencia) → #18 / #21 (vista pública `/u/[slug]` respetando visibilidad) mergeados. HU-003 completa: `app/(app)/perfil`, `app/(app)/u/[slug]`, migración `20260417140000_g4_sprint2_profiles.sql` con `profiles_core/morpho/conditional/technical_football`, `profile_field_visibility` y triggers de defaults.
- **PR #20 (HU-004 tournaments) mergeado a `main`** (commit `c41bf1c`): rutas `app/(app)/tournaments/{page,new,mine}.tsx`, capa de datos `lib/tournaments/api.ts`, schemas Zod, tests unitarios (`tournaments-api.test.ts`, `tournaments.test.ts`), migración `20260425140000_g4_sprint3_tournaments.sql`.
- **Sprint 3 lateral completado**: módulo de aprobaciones de partidos pickup + Centro de Notificaciones (Realtime) + Gestión de Canchas con reservas — migración `20260425000000_sprint3_approvals_venues.sql` (`notifications`, `venues`, `venue_courts`, `venue_reservations`, enum `participant_status` con `requested`/`invited`).

## Decisiones

- HU-004 se entrega como base mínima (CRUD + estados `borrador`/`abierto_inscripciones`/...) sin lógica de inscripción (queda para HU-005 / Sprint 5).
- El módulo lateral de matches/venues/notifications no estaba en el plan original pero se priorizó para validar el MVP v0 con usuarios reales en paralelo al MVP "torneos" del PRD.

## Siguientes pasos

- Iniciar Sprint 4 con foco en hardening de codebase + landing premium para abrir lista de espera.
- Después de hardening, Sprint 5 = HU-005 (Inscripciones) + HU-006 (Resultados/Standings).
