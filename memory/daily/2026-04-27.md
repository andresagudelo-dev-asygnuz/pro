# Diario — 2026-04-27

## Qué se hizo

- **Sprint 4 — Hardening + Landing premium**:
  - PR #22 (`feature/landing-page-premium`): landing high-fidelity con GSAP, secciones (Hero, Challenge, LocalFocus, RegistrationForm), OG/favicon, banner de cookie consent.
  - PR #23: refactor de tournaments UI (mismo branch base que PR #20).
  - PR #24 (`chore/pending-local-updates`): consolida fixes de venues/notifications/match actions, types DB, ESLint warnings/errors, lockfile pnpm.
- Migración `20260427000000_market_validation.sql`: tabla `market_validation_responses` + ruta `/feedback` con `MomTestGame.tsx` para captar señales tempranas y construir lista de espera.
- Múltiples fixes TS de compatibilidad React 19: `participant_status` enum extendido (`requested`/`invited`), transitions en notifications, form actions en venues.
- `tasks/gate-status.md` actualizado a "En curso — Sprint 4".

## Bloqueo / Regresión detectada (post-mortem 2026-04-28)

- El merge de PR #22 (landing premium, commit `9526665` en su forma final) pisó archivos fuera de su scope y **borró toda la implementación de HU-004 Tournaments** (`apps/web/app/(app)/tournaments/*`, `apps/web/lib/tournaments/api.ts`, tests, migración `20260425140000_g4_sprint3_tournaments.sql`). El commit final de `main` (`c15d648`) no contiene esos archivos a pesar de que `c41bf1c` los había agregado.
- Rama `chore/pending-local-updates` (PR #24) pasó CI (4 checks). Sin embargo `main` post-merge presentaba 14 errores ESLint preexistentes en `lib/analytics.ts`, `components/feedback/MomTestGame.tsx`, `components/landing/RegistrationForm.tsx`, `components/match/match-form.tsx`, `components/notifications/notification-list.tsx`, `lib/match/actions.ts`, `tests/lib/match-logic.test.ts` y `app/(app)/admin/venues/page.tsx`. Resolución pospuesta para 2026-04-28.

## Siguientes pasos

- Restaurar HU-004 cherry-pickeando `c41bf1c` sobre `main`, mover migración a `apps/web/supabase/migrations/`.
- Limpiar errores ESLint preexistentes para que `pnpm lint` quede en 0 errors.
- Documentar la regresión en `gate-status.md` + sprints + project-memory.
- Borrar ramas remotas mergeadas (`feature/landing-page-premium`, `jules-g4-sprint2-hu003-final-...`).
