# Diario — 2026-04-28

## Qué se hizo

- **Auditoría de `main` end-to-end**: revisión de commits, PRs (#15–#24), ramas remotas, `tasks/gate-status.md`, `memory/`, migraciones y rutas en `apps/web/app/`.
- **Hallazgo crítico**: confirmada regresión de HU-004 Tournaments en `main`. PR #22 (landing premium) eliminó `apps/web/app/(app)/tournaments/`, `apps/web/lib/tournaments/api.ts`, tests y la migración `20260425140000_g4_sprint3_tournaments.sql`. Verificación: `git ls-tree main 'apps/web/app/(app)/tournaments'` → vacío; `git show --stat 9526665` → todos los archivos de tournaments con líneas `--`.
- **Restauración HU-004**: cherry-pick de `c41bf1c` sobre `main` con resolución de conflictos en `tasks/gate-status.md`. **Migración movida** de `db/migrations/` a `apps/web/supabase/migrations/` (la ubicación correcta para el pipeline Supabase).
- **Limpieza ESLint** (0 errors, 14 warnings de `<img>` y unused imports en componentes de landing — no bloquean CI):
  - `lib/analytics.ts`: `Record<string, any>` y `(window as any).gtag` → tipos `WindowWithGtag` + `Record<string, unknown>`.
  - `components/feedback/MomTestGame.tsx`: 3× `any` tipados, hook fix (`renderThanks` función → componente `<ThanksContent />` para cumplir `react-hooks/rules-of-hooks`).
  - `components/landing/RegistrationForm.tsx`: `catch (error: any)` → `unknown`.
  - `components/match/match-form.tsx`: tipo `VenueWithCourts` y `VenueCourt` exportados.
  - `components/notifications/notification-list.tsx`: tipo `NotificationData` con `[key: string]: unknown`.
  - `lib/match/actions.ts`: `(court.venue as any).name` → guard de array vs objeto.
  - `tests/lib/match-logic.test.ts`: `cb: any` → firma tipada.
  - `app/(app)/admin/venues/page.tsx`: `c: any` → `{ id, name, capacity_players }`.
  - `app/(app)/tournaments/{page,mine/page}.tsx`: `t: any` → `t: TournamentRow` (nuevo tipo exportado desde `lib/tournaments/api.ts`).
- **Validación local**:
  - `pnpm lint` → 0 errors, 14 warnings.
  - `pnpm typecheck` → OK.
  - `pnpm test` → 130/130 PASS.
  - `pnpm build` → OK; rutas `/tournaments`, `/tournaments/mine`, `/tournaments/new` presentes en el output de `next build`.
- **Documentación**:
  - `memory/daily/2026-04-26.md`, `2026-04-27.md`, `2026-04-28.md` (esta entrada) creados.
  - `memory/project-memory.md` ampliado con post-mortem del Sprint 4.
  - `tasks/gate-status.md` reflejando Sprint 4 + restauración HU-004.
  - `tasks/sprint-week-03.md` y `tasks/sprint-week-04.md` creados (sprint 3 lateral + sprint 4 hardening).
  - `tasks/hu/HU-004.md` actualizado con evidencia (PR + commits + tests).
  - `tasks/current-gate.txt` permanece en `4` (Sprint 5 abrirá HU-005 + HU-006 antes de pasar a G5).

## Decisiones

- Estrategia de tipos para limpiar ESLint: usar `unknown` + narrowing en lugar de `// eslint-disable-next-line` para no acumular deuda. La limpieza es preexistente al cherry-pick y se aprovechó para dejar `pnpm lint` en verde como criterio de cierre del Sprint 4.
- El sprint 4 se da por cerrado una vez merge este PR (HU-004 restaurada + lint limpio + docs sincronizada).
- Sprint 5 abre con HU-005 (Inscripciones) y HU-006 (Resultados/Standings) en serie, no en paralelo, para no repetir el patrón de regresión por merges grandes.

## Próximos pasos (Sprint 5)

1. **HU-005 — Inscripciones a torneos** (RF-004): tablas `teams`, `team_members`, `tournament_registrations`; trigger de capacidad análogo al de `matches`; validación de verificación de edad (RF-007) por miembro; lista de espera; cancelación.
2. **HU-006 — Resultados + tabla de posiciones** (RF-005): `tournament_matches`, `match_events`, vista materializada `standings` con `REFRESH MATERIALIZED VIEW CONCURRENTLY` (índice único `standings_unique_row` ya modelado en `db/data-model.md`); reflejo de stats básicas en perfil del jugador respetando visibilidad.
3. **Higiene**: borrar ramas remotas mergeadas; agregar regla pre-merge para evitar regresiones masivas (revisar diff cuando un PR de UI/landing toca > N archivos de dominio diferente).
