# Tasks: Staging Schema Separation

- [x] Add runtime schema config for Supabase client.
- [x] Switch realtime listeners from hardcoded `public` to runtime schema variable.
- [x] Expose `staging` in `supabase/config.toml`.
- [x] Add migration to create and grant `staging` schema.
- [x] Document setup and risks for staging schema usage.
